package otaku.info.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import otaku.info.entity.AffeliUrl;

public interface AffeliUrlRepository extends JpaRepository<AffeliUrl, Long> {
}
